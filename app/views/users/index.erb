
<div>
  <h1>Snack Lovers</h2>
  <ul class="no-list">
    <% @users.each do |user| %>
      <li><a href="/users/<%= user.id %>"><%= user.username.capitalize %></a></li>
      <!-- show user's last score ??? -->
    <% end %>
  </ul>

</div>
